We have pulled the latest Landy's Pro code into Replit.

The deployment target is:

* Branch: `main`
* Expected HEAD: `22e91a0`
* App directory: `artifacts/landys-pro/`
* Pre-pivot rollback reference: `708b4a9`

Your job is to make this Replit project **fully deployment-ready** so that when you are finished, I should only need to click **Deploy** in Replit.

Do NOT deploy the application yourself.

Do NOT make product changes or refactor working business logic.

Do NOT expose any secret values in your response.

---

# 1. VERIFY THE CHECKOUT

First verify:

* current branch is `main`
* HEAD is `22e91a0`
* working tree does not contain unexpected local modifications
* migration `20260826140000_success_fee_pivot` exists
* the application directory is `artifacts/landys-pro/`

If HEAD is not `22e91a0`, stop and report it before doing anything destructive.

---

# 2. INSPECT THE CURRENT REPLIT ENVIRONMENT

This project existed on Replit before this pivot.

Do NOT blindly recreate or replace existing secrets.

Inspect the current application code and determine every environment variable actually required for:

* PostgreSQL / Prisma
* Clerk
* Wix / Landys.co estimate intake
* Wix contractor synchronization
* Stripe
* Stripe webhook handling
* email notifications
* SMS notifications
* cron authentication
* public application URL / base URL
* any other production integration currently referenced by the code

At minimum, specifically verify the presence/configuration of:

* `DATABASE_URL`
* Clerk publishable + secret keys
* Wix/Landys.co integration/authentication variables
* Stripe publishable key
* Stripe secret key
* `STRIPE_WEBHOOK_SECRET`
* email provider credentials
* SMS provider credentials
* `CRON_SECRET`
* any app/public base URL variable actually used by the code

Classify every required variable as:

* PRESENT
* MISSING
* PRESENT BUT NEEDS REVIEW

Never print the actual value.

If a required secret is missing and cannot safely be generated by you, DO NOT invent one except for an internal secret such as `CRON_SECRET`, which may be securely generated and saved in Replit Secrets if appropriate.

For third-party credentials such as Stripe, Clerk, Wix, Twilio, Resend, etc., never invent credentials.

If any critical third-party credential is missing, continue with everything else that can safely be completed, then clearly report only what I need to provide.

---

# 3. VERIFY DATABASE TARGET BEFORE MIGRATION

Before running Prisma migrations:

* confirm `DATABASE_URL` exists;
* confirm Prisma can connect successfully;
* identify the database/environment it targets as far as can be safely determined without exposing credentials;
* verify this is the database intended for this Replit production deployment;
* inspect Prisma migration status.

Do not reset the database.

Do not run `prisma migrate dev`.

Do not drop schemas or tables.

Do not delete legacy wallet/pay-per-lead tables.

The previous pay-per-lead schema is intentionally allowed to remain dormant.

---

# 4. APPLY PRODUCTION PRISMA MIGRATIONS

From:

`artifacts/landys-pro/`

run the repository's production migration command:

`pnpm run prisma:deploy`

If that script is not the correct command according to `package.json`, use the actual production-safe equivalent and explain why.

Confirm:

* all previous migrations remain intact;
* `20260826140000_success_fee_pivot` is applied;
* there are no failed/pending migrations afterward;
* Prisma client generation is correct if generation is required.

If migration fails, investigate the cause.

Do NOT use destructive repair/reset commands just to force it through.

---

# 5. VERIFY SUCCESS-FEE DATABASE STRUCTURE

After migration, verify that the expected success-fee structures exist and are usable, including the implementation's equivalents of:

* LeadMatch job outcome fields
* SuccessFee
* SuccessFeeTier
* FollowUpToken
* LandownerConfirmation
* new AppSetting values

Verify default tier configuration exists or can be initialized correctly:

* Small: under $10,000 → 5%
* Medium: $10,000–$24,999 → 4%
* Large: $25,000+ → 3%

These values must remain editable later through Admin Settings.

Do not manually duplicate seed data if the application/migration already handles it.

---

# 6. VERIFY APPLICATION SETTINGS / DEFAULTS

Check that the new product defaults are correctly initialized:

## Opportunity acceptance

Default:

* maximum accepted contractors = 3

Admin must be able to change this.

Also verify:

* Unlimited acceptance mode exists and defaults to the intended safe value (normally OFF unless code specifies otherwise).

## Follow-ups

Verify that sensible default follow-up settings exist for:

* contractor outcome follow-up
* contractor payment follow-up
* retry when contractor says "Not yet"

Do not invent complex workflow logic.

Use the defaults already defined in the implementation.

---

# 7. LEGACY PAY-PER-LEAD SAFETY CHECK

Verify the active application no longer depends on:

* wallet balance
* wallet top-up
* lead purchasing
* lead charge
* pay-to-unlock
* insufficient wallet balance
* old lead pricing matrix

Legacy database structures may remain.

Verify there is no active contractor/admin UI path capable of accidentally triggering the old wallet recharge, restitution or refund workflows.

Do NOT delete the legacy schema.

---

# 8. STRIPE CONFIGURATION CHECK

Inspect the current implementation and verify production readiness for the NEW flow:

Landowner → Contractor:

* always outside Landy's
* never processed by Stripe through Landy's

Contractor → Landy's:

* success fee paid through Landy's Pro
* Stripe handles online success-fee payments
* Admin can manually mark offline/check payments as paid

Verify:

* Stripe keys are present
* webhook route is correctly configured in the application
* webhook authentication/signature verification is intact
* success-fee webhook handling is wired
* event idempotency remains intact
* the current webhook endpoint can support the success-fee flow

Determine which Stripe webhook events are actually required by the implementation.

Check whether the currently configured `STRIPE_WEBHOOK_SECRET` corresponds to the webhook endpoint expected by this deployment as far as possible.

Do NOT create duplicate Stripe endpoints unnecessarily.

If an external Stripe dashboard change is still required after deployment because the final public deployment URL is needed, report it explicitly as a POST-DEPLOY action rather than blocking the rest of the setup.

---

# 9. FOLLOW-UP CRON SETUP

The application now has:

`GET /api/cron/follow-ups`

Authentication:

`Authorization: Bearer <CRON_SECRET>`

The route was intentionally added to the Clerk public-route exclusions in commit `22e91a0`.

Inspect the existing Replit architecture and the existing `expire-leads` scheduling approach.

Prepare the simplest supported configuration for calling:

`/api/cron/follow-ups`

once per hour.

Requirements:

* reuse the existing application;
* do NOT create a second application;
* do NOT create an unnecessary second server;
* use the same Replit deployment if possible;
* securely use `CRON_SECRET`;
* follow the existing cron authentication pattern.

If Replit allows the cron/scheduled request to be fully configured before deployment, configure it.

If the final deployed URL is technically required before the cron can be created, prepare everything else and clearly mark only that final cron attachment as POST-DEPLOY.

Do not fake successful cron configuration if Replit requires a deployed URL first.

---

# 10. BUILD / RUNTIME CONFIGURATION

Inspect the Replit deployment/run configuration.

Verify:

* install/package manager is correct (`pnpm`)
* working directory is correct
* build command is correct
* production start command is correct
* `$PORT` is respected
* host binding is production-compatible (`0.0.0.0` where required)
* Prisma generation/build ordering is correct
* there is no stale command from a previous application structure

Do not change commands unnecessarily if the existing deployment configuration is already correct.

Run a full production build locally inside Replit.

The build must pass before declaring deployment readiness.

---

# 11. APPLICATION HEALTH CHECK BEFORE DEPLOY

Start the app in the Replit development/workspace environment against the configured database and verify as much as possible without deploying.

Check at minimum:

PUBLIC

* `/`
* `/sign-in`

CONTRACTOR

* dashboard loads
* opportunities loads
* jobs loads
* fees loads

ADMIN

* admin dashboard loads
* settings loads
* success fees loads
* confirmations loads

Do not create fake production business data unnecessarily.

If test/seed data already exists and is safe to use, perform a basic navigation smoke check.

Verify there are no obvious 500 errors related to:

* Prisma
* missing migrations
* missing environment variables
* Clerk
* success-fee schema
* legacy wallet removal

---

# 12. FINAL PRE-DEPLOY VALIDATION

Run:

* TypeScript/typecheck
* production build
* relevant automated tests, preferably the existing full test suite if practical

The previous baseline was:

* Typecheck: PASS
* Tests: 192/192 PASS

If the numbers change because new files/tests exist, report the actual result.

Do not silently ignore failures.

---

# 13. DO NOT DEPLOY

When everything possible before deployment is complete:

STOP.

Do not click Deploy.
Do not trigger a production deployment.

The user will do that manually.

---

# FINAL RESPONSE FORMAT

Return a concise **DEPLOYMENT READINESS REPORT** with exactly these sections:

## Git

* Branch
* HEAD
* Working tree status

## Database

* Connection: READY / BLOCKED
* Migration status
* `20260826140000_success_fee_pivot`: APPLIED / NOT APPLIED

## Secrets

Provide a table:

| Variable / Integration | Status                           |
| ---------------------- | -------------------------------- |
| Database               | READY / MISSING                  |
| Clerk                  | READY / MISSING                  |
| Wix/Landys.co          | READY / MISSING                  |
| Stripe                 | READY / MISSING                  |
| Stripe webhook         | READY / NEEDS POST-DEPLOY ACTION |
| Email                  | READY / MISSING                  |
| SMS                    | READY / MISSING                  |
| CRON_SECRET            | READY / MISSING                  |
| Public/base URL        | READY / NEEDS POST-DEPLOY ACTION |

Do not reveal values.

## Success-fee configuration

* Acceptance default
* Unlimited mode
* Fee tiers
* Follow-up settings

## Runtime

* Build command
* Start command
* Production build result

## Cron

* READY, or
* exact post-deploy action still required

## Verification

* Typecheck result
* Tests result
* Local Replit smoke-check result

## Blocking issues

List ONLY items that genuinely prevent clicking Deploy.

## Post-deploy actions

List ONLY actions that technically cannot be completed until the public deployment exists.

Finally state exactly one of:

`READY TO CLICK DEPLOY`

or

`NOT READY TO DEPLOY — [short reason]`
