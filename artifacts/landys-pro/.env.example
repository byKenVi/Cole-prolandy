# ─────────────────────────────────────────────────────────────
# Landy's Pro — Environment variables (EXAMPLE / TEMPLATE)
# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
# NEVER commit real secrets. All placeholder values below are safe/fake.
# The app runs fully on MOCK integrations while the *_MOCK flags are "true".
# ─────────────────────────────────────────────────────────────


# ═══════════════════════════════════════════════════════════
# CORE
# ═══════════════════════════════════════════════════════════
# Do not define NODE_ENV; Next.js sets it for each command.
# Public base URL of the app; used to build tokenized SMS accept links.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Explicit runtime tier:
#   local       = Cursor QA on a dedicated Supabase DEV project
#   development = default local Next without local marker
#   staging     = separate staging Repl
#   production  = Replit production
LANDYS_ENV="development"
# Required only in the separate staging Repl:
# STAGING_PUBLIC_URL="https://landys-pro-staging.example.replit.app"

# Local Cursor QA (see docs/local-qa.md and .env.local.example):
# LANDYS_ENV="local"
# LOCAL_SUPABASE_PROJECT_REF="your-dedicated-dev-project-ref"
# LOCAL_NOTIFICATION_EMAIL="qa@localhost.test"
# LOCAL_NOTIFICATION_PHONE="+15005550006"
# LOCAL_ADMIN_EMAIL="admin@localhost.test"
# LOCAL_CONTRACTOR_EMAILS="contractor1@localhost.test,contractor2@localhost.test,contractor3@localhost.test,contractor4@localhost.test"


# ═══════════════════════════════════════════════════════════
# DATABASE — PostgreSQL
# ═══════════════════════════════════════════════════════════
# Replit supplies this automatically for its isolated Development/Production DBs.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"
# Dedicated staging lifecycle commands read this rather than DATABASE_URL.
# STAGING_DATABASE_URL="postgresql://STAGING_USER:STAGING_PASSWORD@STAGING_HOST:5432/postgres"


# ═══════════════════════════════════════════════════════════
# MEDIA — Replit App Storage (provisioned automatically)
# ═══════════════════════════════════════════════════════════
# DEFAULT_OBJECT_STORAGE_BUCKET_ID, PRIVATE_OBJECT_DIR, and
# PUBLIC_OBJECT_SEARCH_PATHS are supplied by Replit App Storage.


# ═══════════════════════════════════════════════════════════
# AUTH — Clerk
# ═══════════════════════════════════════════════════════════
# REQUIRED = "clerk" in production. Any other value uses insecure DEV auth and the
# app REFUSES to boot in production (fail-closed). Use "clerk" or "dev".
AUTH_MODE="dev"
# Clerk API keys (Dashboard → API Keys). Required when AUTH_MODE=clerk.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="<your-clerk-publishable-key>"
CLERK_SECRET_KEY="<your-clerk-secret-key>"
# Comma-separated admin emails; a signed-in Clerk user with one of these is admin.
ADMIN_EMAILS="admin@example.com"
# Clerk hosted sign-in/up routes and post-auth redirects.
# IMPORTANT: fallback must be /post-auth (not /home) so admins are routed to
# /admin after sign-in. The SignIn/SignUp components also force /post-auth.
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/post-auth"
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL="/post-auth"


# ═══════════════════════════════════════════════════════════
# PAYMENTS — Stripe (contractor → Landy's success fees)
# ═══════════════════════════════════════════════════════════
# STRIPE_MOCK="true" simulates Stripe; "false" uses the real API with the keys below.
STRIPE_MOCK="true"
# Stripe secret key (test starts sk_test_, live starts sk_live_). Required when not mocked.
STRIPE_SECRET_KEY="<your-stripe-secret-key>"
# Webhook signing secret (Stripe → Developers → Webhooks). Verifies top-up events.
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxxxxxxxxxxxxxx"

# Replit Preview/Development obtains an isolated test-mode secret/publishable key
# from the native Stripe integration and creates a managed Preview webhook.
# Do not add Development Stripe credentials to Replit Secrets. STRIPE_* remains
# for local Cursor/staging and the existing Production fallback only.


# ═══════════════════════════════════════════════════════════
# SMS — Twilio (lead notifications + tokenized accept link)
# ═══════════════════════════════════════════════════════════
# TWILIO_MOCK="true" logs SMS to console instead of sending.
TWILIO_MOCK="true"
# Twilio account SID. Required when not mocked.
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxx"
# Twilio auth token. Required when not mocked.
TWILIO_AUTH_TOKEN="xxxxxxxxxxxxxxxxxxxxxxxx"
# Sending phone number in E.164 (trial accounts use this). Do NOT use TWILIO_FROM_NUMBER.
TWILIO_FROM="+15555550123"
# Staging redirects every outgoing SMS here. Missing override = fail closed.
# STAGING_NOTIFICATION_PHONE="+15005550006"
# Local Cursor QA redirects here when LANDYS_ENV=local:
# LOCAL_NOTIFICATION_PHONE="+15005550006"
# Optional. Only set a real MGxxxx from Twilio Console (A2P). Leave unset for trial.
# TWILIO_MESSAGING_SERVICE_SID="MGxxxxxxxxxxxxxxxxxxxxxxxx"


# ═══════════════════════════════════════════════════════════
# EMAIL — Resend (lead + acceptance notifications)
# ═══════════════════════════════════════════════════════════
# RESEND_MOCK="true" logs emails to console instead of sending.
RESEND_MOCK="true"
# Resend API key. Required when not mocked.
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxx"
# From address; must be a verified domain in Resend.
RESEND_FROM="leads@example.com"
# Staging redirects every outgoing email here. Missing override = fail closed.
# STAGING_NOTIFICATION_EMAIL="qa@example.com"
# Local Cursor QA redirects here when LANDYS_ENV=local:
# LOCAL_NOTIFICATION_EMAIL="qa@localhost.test"


# ═══════════════════════════════════════════════════════════
# WIX ESTIMATE INTAKE — server-to-server
# ═══════════════════════════════════════════════════════════
# Fail-closed switch for POST /api/integrations/wix/estimate-requests.
WIX_ESTIMATE_INTEGRATION_ENABLED="false"
# Long random server-only bearer secret shared with the Wix backend developer.
WIX_ESTIMATE_API_SECRET="replace-with-a-long-random-string"

# Wix contractor sync (AllContractors collection)
WIX_SITE_ID="your-wix-site-id"
# Use the exact Authorization header value verified against live Wix (may or may not include "Bearer ")
WIX_API_AUTHORIZATION="your-wix-api-authorization-header-value"
WIX_CONTRACTOR_COLLECTION_ID="AllContractors"


# ═══════════════════════════════════════════════════════════
# SECURITY
# ═══════════════════════════════════════════════════════════
# Estimate-form spam protection (honeypot + rate limit). Configurable in dev;
# ALWAYS forced ON in production regardless of this flag.
FORM_SPAM_PROTECTION="true"
# Max estimate submissions per IP per hour.
FORM_RATE_LIMIT_PER_HOUR="10"
# Bearer secret for the cron expiry-sweep endpoint. REQUIRED in production — the
# endpoint fails closed (401) if this is unset in prod.
CRON_SECRET="replace-with-a-long-random-string"
